<%= form_with(model: type_of_event, local: true, class: 'form-horizontal') do |form| %>
  <% if type_of_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_of_event.errors.count, "error") %> prohibited this type_of_event from being saved:</h2>

      <ul>
      <% type_of_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= form.text_field :name, class: 'form-control', type: 'text' %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= link_to 'Back', type_of_events_path, class:'btn btn-danger' %>
      <%= form.submit class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>